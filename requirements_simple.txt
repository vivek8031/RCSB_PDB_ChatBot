# Simple RAGFlow ChatBot UI Requirements

# Core Dependencies
streamlit>=1.28.0
ragflow-sdk>=0.19.0

# Standard library dependencies (usually included with Python)
# json, os, time, datetime, typing, uuid, dataclasses

# Optional: For better development experience
python-dateutil>=2.8.0