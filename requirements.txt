# RCSB PDB Multi-User Research ChatBot Requirements

# Core Dependencies
streamlit>=1.28.0
ragflow-sdk>=0.20.4
openai>=1.0.0
python-dotenv>=1.0.0

# UI Enhancement Dependencies
# Using Streamlit's built-in st.markdown for markdown rendering

# Standard library dependencies (included with Python)
# json, os, time, datetime, typing, uuid, dataclasses, pathlib

# Optional: For better development experience
python-dateutil>=2.8.0

# Testing Framework Dependencies
rich>=13.0.0
crewai>=0.20.0
langchain-openai>=0.1.0